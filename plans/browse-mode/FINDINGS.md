# Findings log

**What the build taught, especially where reality contradicted the design.**

**Read this before touching a task**, and read it whole before anything that only a person can
verify. It is the only place a hand-verification is written down: the test command is the only
evidence a session can produce on its own, so a ✅ line here is the *entire* record that
something was seen working for real.

**Newest first. About forty words a row.**

Legend: 🐞 a defect found · ✅ verified by hand with the user · 📌 worth knowing ·
🔄 a decision the user changed.

| Date | Finding | Consequence |
|---|---|---|
| 2026-09-04 *(T12 review)* | 📌 **`one-dark` is embedded in the binary** — `colorschemes/one-dark.micro`, micro 2.0.15 — and the flag writes nothing: `~/.config/micro/settings.json` still does not exist after the user's hands-on run. | Confirms the scheme independently of the hands-on check, and proves the launch-line route leaves their own micro untouched. Both halves of DESIGN §7's rule now have evidence. |
| 2026-09-04 *(T12 review)* | 📌 **An invalid scheme stops at an interactive `Press enter to continue`**, after printing the refusal (probed with no TTY, where micro then panicked). | So the "1 s healer would relaunch it forever" written in §7 and in `viewerCommand`'s comment is **unproven** — a wedged pane reading as *running* is as likely. Untested in a real pane; moot while the name is one micro ships. |
| 2026-09-04 *(T12)* | 📌 **The live cockpit is pointed at THIS WORKTREE, not the main checkout** — `config.lua`'s `repo` and the `~/.wezterm.lua` symlink both name it, since T13's hands-on half repointed them (08:44) and did not put them back. | PROGRESS's header warning reads the other way round and is **stale**. Left as it is deliberately: main has no browse mode, so repointing would take the feature away mid-decision. The user's call, with the branch's fate. |
| 2026-09-04 *(T12)* | ✅ **`one-dark` loads and the user took it.** They ran the reader's own launch line by hand; micro started, so the name is a scheme it ships — an unknown one is a startup refusal, not a fallback. | **T12's hands-on half passes.** The scheme is settled and rides on the launch line; nothing was written into `~/.config/micro/`. |
| 2026-09-04 *(T12)* | 🔄 **The user wanted VSCode-shaped tabs — a light active tab with a small `[x]` — and settled for the colours alone.** micro's tab bar layout is fixed: its four tab options reverse the active tab, invert the text, shorten the label; none adds a close button. | **Declined as a plan, not deferred**: the `[x]` would mean replacing the reader (T04 onward is micro-specific) or the cockpit drawing its own tab bar. §8 records it. Note the active tab already inverts, via micro's `tabreverse`. |
| 2026-09-04 *(T13 review)* | 📌 **A second copy of the `ps` parser lives in `cockpit-strip.mjs`** (line 80), still last-wins. It only *labels* a terminal in the strip, so a same-group child mislabels one row — cosmetic. | Out of T13's scope, left alone. Worth knowing that "one parser, not two" holds inside `cockpitd.mjs` only: a future fix to the daemon's reading will not reach the strip's. |
| 2026-09-04 *(T13)* | ✅ **The healer no longer types broot's launch command into broot's filter box.** Verified by hand on the review branch with the cockpit repointed at it: Enter pressed on **a dozen files in a row**, no filter-box text, no tree jump. | **T13's hands-on half passes** — the half no test can produce, since the race is timing and the reproduction was the user's. The any-of-the-group predicate closes the window last-wins opened; §11c''''' defends it in the suite. |
| 2026-09-04 *(T13)* | 📌 **`foregroundComm`'s last-wins rule is now defended by nothing.** Its only caller is `terminalIsIdle`, and both §9 and §9b answer `ps` with a **single** foreground line, so first-wins and last-wins agree. | Pre-existing, not introduced by T13 — but the split into `foregroundComms` makes it easy to lose. A two-line answer for a **terminal** pane would pin it; needs an agent `cwd` move to reach the idle check, so it was left rather than built here. |
| 2026-09-04 *(T11 review)* | 📌 **The footer's focus-borrow can undo the Enter activation.** `pinHeight` records the active pane, activates itself to resize, then restores it — an Enter landing inside that ~100ms window ends back in the tree. | **Not fixed, not T11's**: the borrow is the strip's own mechanism. Needs a *new* drift height after a resize, so it is rare and self-correcting. If ever seen, `pinHeight` should re-read the active pane after the resize rather than trust the one it recorded. |
| 2026-09-04 *(T11)* | 🐞 **The healer types broot's launch command into broot's own filter box, intermittently, on Enter.** Seen by the user; reproduced and measured. **While broot runs the Enter verb its child is on the same tty**, and `foregroundComm` takes the *last* foreground line: `SNs+ broot · SN+ /bin/sh · RN+ ps`, so it answers the child. broot draws no frame and titles itself `cd`, so that one signal decides. | **Not T11's doing** — the push always spawned `cockpit-open`; T11's extra `wezterm` spawn only widens the window the 1s healer can land in. **T08's fix is right and incomplete**: it asked *which* process is in front, when the question is *whether any* of the foreground group is `broot`/`micro`/`revdiff`. **FIXED in T13** — `foregroundComms`, defended by §11c''''' (five primes); hands-on half closed the same day (row above). |
| 2026-09-04 *(T11)* | ✅ **Enter takes the cursor into the reader, and the return trip needs nothing new.** The user: cursor lands in the reader, `⌘⌥←` gets back to the tree. **`Alt+Enter` is confirmed not wanted** now that a tab bar has been seen — option B is closed, not deferred. | **T11's hands-on half passes**, all three questions. The stay-in-the-tree variant can come out of §8's "out of scope" holding pattern: it was declined once without the evidence and once with it. |
| 2026-09-03 *(T07)* | ✅ **The whole of browse mode is drivable without the mouse.** The user's words: *"the cockpit can be operated mouse-less."* Entering, filtering, Enter, `c/`, leaving and re-entering, switching agents — keyboard throughout. | **T07's flagship check and the last question in the plan**, so **T07 closes**. It is the point of the shape §1 chose and nothing else stood in for it. Browse mode is now person-proven end to end. |
| 2026-09-03 *(T07)* | ✅ **The reader's tabs and the tree's position survive both a park and an agent switch** — three tabs still there, tree where it was left. And **`c/` + Enter lands on the matching line**, which micro puts at the top of the viewport. | The last of T07's checks, so **T07 closes**. The park's untouched-PTY claim (2026-08-29, "asserted nowhere") is person-proven, and so is the `c/` handover a headless probe could only see as argv. |
| 2026-09-03 *(T07)* | 🔄 **The user reversed the focus rule: Enter must take the cursor INTO the reader**, so they land in the file they opened. DESIGN §3.1 and T07 both say it stays in the tree. Taken with the cost in view — stacking tabs now needs a `⌘⌥←` between Enters. | **T11.** A stay-put `Alt+Enter` was offered and deliberately **not** built: they had never seen a tab bar when asked, so the judgement was not available to them. `⌘⌥←`/`⌘⌥→` already walk between the halves, so focus in the reader strands nothing. |
| 2026-09-03 *(T07)* | 📌 **Three open tabs read as none: micro's default colours draw the tab bar light-on-light.** The user had them open the whole time and could not see them — "white tabs on a white strip". No `~/.config/micro/settings.json` exists, so this is micro straight out of the box. | **T12**, on the user's yes. The cockpit launches `micro -readonly true` and passes no scheme, so a `-colorscheme` on that line fixes it without ever writing into their own micro config — the same restraint the `⌥o` verb got. |
| 2026-09-02 *(T10 review)* | ✅ **The user confirms the 80/20 tree reads right** — the width question DESIGN §5.1 reserved for a person is now closed both ways: 60 judged too wide, 80 judged correct. | T10 ✅ on both halves. Measured on a headless mux at the same review: the tree is **63 columns of 319** where revdiff's own list was 65, and 23 of 120 — a share, so it tracks at any size. |
| 2026-09-02 *(T10 review)* | 🐞 **Six suite labels and code comments still said "60%" while asserting `--percent 80`.** A check's label is what a failure prints, so a red run would have named the wrong number; the daemon also still documented the pair returning "at 47/72". | Labels and comments corrected, assertions untouched (they were already right). The stub suite asserts the *string*: the geometry it is supposed to buy was never checked, so this review probed it on a real mux — slot filled exactly, identical widths across a park. |
| 2026-09-02 *(T09 review)* | 🐞 **A refused `broot --send` is invisible in its effects**, so the guard that stops the fence questioning a quit or still-starting browser was defended by nothing — firing it unconditionally left 365 checks green. | New 11c'''' watches $CALLS in three bounded windows instead. Probed live: `:focus` takes a path **with spaces**, and a dead socket exits **1** with the error on stderr, so `brootSend` really does take its null path. |
| 2026-09-02 *(T08 review)* | 🐞 **The `ps` stub answered in bare names; a real `ps -o comm=` gives a homebrew binary its FULL PATH.** So the daemon's basename reduction was defended by nothing: deleting it left all 365 checks green while browse mode broke on the machine exactly as before. | The stub modelled the assumption, not the machine — T08's own class of error. 11b' now answers `/opt/homebrew/bin/broot` for the browser and bare `micro` for the viewer; the mutation goes red. Measured: `ps -p <pid> -o comm=` → `/opt/homebrew/bin/node`. |
| 2026-09-02 *(T07)* | ✅ **The `--conf` layering does NOT shadow the user's own keys** — verified by hand: `⌥p` still reaches broot inside the cockpit. Also ✅ **the redraw on return from parking is acceptable**, not a glitch. | The last two open questions from DESIGN §5.1. The cockpit binds only `enter` (twice), so only `enter` was ever at risk; that is now evidence rather than reasoning. |
| 2026-09-02 *(T07)* | 📌 **`⌥o` does nothing, and it is the USER's binding that is wrong, not ours.** Measured: `:panel_right` on a **file** opens a preview (contents shown) but on a **directory** opens a second tree; `:panel_left_no_open` only moves focus — the panel stays. `:close_panel` is what closes it. | Their `~/.config/broot/verbs.hjson` binds `⌥o` to `:panel_left_no_open`. Not the cockpit's to fix — the cockpit never writes into `~/.config/broot/` — so it was reported, with the one-line change, for them to decide. |
| 2026-09-02 *(T07)* | ✅ **VERIFIED BY HAND WITH THE USER, in a real cockpit on the review branch.** The tree is **usable** (the T08 defect is really gone). The **fence helped** rather than fought — the snap-back read as help, and descending into a subdirectory was left alone, as designed. **Focus never left the tree.** The footer still fits on one line with the fourth label. | The first evidence that any of browse mode works for a person. T08 and T09 are person-proven as well as test-proven; T04's focus rule is confirmed on real panes. The heals were seen too — three quits, three relaunches within a second, in the daemon log. |
| 2026-09-02 *(T07)* | 🔄 **The user judged the 47/72 split wrong: the tree is too wide.** They asked for it to match **revdiff's** file list instead. Measured: revdiff's `--tree-width` is *"units (1-10, default 2 of 10)"* — a **share**, not a column count — and its box was 65 columns of a 319-column pane, 20.4%. | `BROWSE_VIEWER_PERCENT` 60 → **80**, so the tree tracks revdiff's 20% at every window size rather than a width measured on its own. The plan asked exactly this question of T07 (DESIGN §7) and this is the answer. |
| 2026-09-02 *(T09)* | 📌 **broot cannot be confined to a directory — checked before anything was built.** v1.59 has no jail option (only cosmetic root flags), and a verb of ours named `parent`, first in the `--conf` chain and loaded without error, did **not** shadow the built-in `:parent`: the root still moved up. Blocking keys would not do it either — `:parent` can be typed. | So the fence checks where broot **ended up**, not how it got there: `--listen` at launch, then `--get-root` (23–30ms) and `--cmd :focus <worktree>` once a second. Closes every route at once. A dead broot answers *Connection refused* immediately, and a socket name is reusable after a crash — both measured. |
| 2026-09-02 *(T07)* | 🐞 **Found by hand, and it made browse mode unusable: the pane TITLE never says `broot`.** A shell with a `preexec` hook — the user's zsh — writes the title itself, to the command's **first word**. Both halves launch `cd <worktree> && …`, so the title is `cd`, always. Every healthy half read as a quit shell and the healer typed broot's own command line into broot's filter box every ~3s, resetting the tree. | The `script(1)` probe below was right that neither program sets a title, and the conclusion drawn from it was wrong: WezTerm falls back to the process name only when *nothing* sets one, which on a real machine never happens. Detection now asks the OS for the tty's foreground process (`ps -t`, as `terminalIsIdle` already did). Measured live: title `cd`, `ps` `S+ revdiff`. |
| 2026-08-30 *(T06 review)* | 🔄 **The user added two steps to T07.** As written, the only hands-on task never quit either half, so T06 — the recovery task — would have closed with nobody having seen it work, and the title signal every heal fires on is stubbed in every test. | T07 gains steps **10 and 11** (quit the reader, quit the tree, hands off, watch) and two questions. Asked with a recommendation at T06's review; answered the same day. |
| 2026-08-30 *(T06 review)* | 🐞 **The reaper killed a browse-mode agent's browser twice** — once through `disposePair`, then again through the `diffs` entry that names the same pane. A real `wezterm cli` fails the second kill, and any failed call drives `repairMuxSocket`. | So a routine reap relinked the mux socket and spent the repair cooldown a genuine socket failure needs. Fixed by noting the pair's panes before the disposal; §11p now asserts the kill count is exactly one. |
| 2026-08-30 *(T06 review)* | 🐞 **"Both halves come back in the SAME pass" was not what §11c'' checked.** It waited a flat 5s, which a per-agent cooldown satisfies too — the second half is held 3s, then healed, not abandoned. Measured: keyed per agent, the section went green. | The per-pane clock is still the right answer (no 3s stall), but the claim was undefended. The wait now ends at the browser's heal and the viewer's is required one second later. |
| 2026-08-30 *(T06 review)* | 🐞 **A reaped agent's per-agent records outlived it unless it still held a pane in `diffs`** — `diffModeByAgent` and all four `diffLaunched*`. That is exactly the failed-`leaveBrowse` case the candidate list was widened for. | Fixed: the deletes moved out of the `diffs` branch. Memory only, and invisible from outside the daemon, so this one rests on reading rather than on a test. |
| 2026-08-30 *(T06 review)* | 📌 **A quit viewer stays advertised in `panes.json` until the heal lands** (≤4s). An Enter in broot inside that window types `open <path>` into a bare shell, and macOS opens a GUI app over the terminal — the outcome §7 went out of its way to avoid for binary files. | Left alone: unpublishing and republishing around a heal is new behaviour, not T06's. The window is short and only a person can open it. A follow-up task if it is ever actually seen. |
| 2026-08-30 *(T06)* | 📌 **A miss that does not reap leaves no trace**, so "two consecutive misses are required" was assertable only through a ~350 ms timing window — the exact flake this suite is warned about. The reaper now logs `agent X missing (1/2); not reaping yet`. | Turns a race into an ordering assertion: §11p checks the strike line grew and came *before* the reap. Same trick as the browse halves' status log — the correct behaviour is to do nothing, so the log is the only witness. |
| 2026-08-30 *(T06)* | 📌 **The reaper only looked at `terminals` and `diffs`.** An agent whose `leaveBrowse` could not hand the slot back is dropped from `diffs` while its pair and revdiff stay parked — four live panes nobody can reach, for the life of the window. | Candidates now union `browsePairs` and `parkedDiffs` too. The `visibleDiff` exclusion is unchanged, so an agent holding the slot is still never reaped and the slot cannot be emptied by one. |
| 2026-08-30 *(T06)* | 🐞 **PROGRESS overstated the crude fallback: a dead VIEWER never rebuilt the pair.** `showDiff` already nulls a gone viewer and keeps the browser, so the next entry spawns a fresh micro beside it. Only a dead **browser** disposes the pair — the viewer cannot hold the slot alone. | Nothing to fix; T06's healing is about a half quit to a *shell*, not a pane that is gone. Recorded so a reviewer does not go looking for a rebuild-on-dead-viewer path that was never there. |
| 2026-08-30 *(T06)* | 📌 **The 47/72 split after a double heal is not assertable here.** The wezterm stub models the pane tree and titles, not geometry. What is asserted instead is that neither heal kills a pane, splits one, or moves focus, and that both halves stay in the cockpit tab. | The healer types into two existing panes and touches no geometry at all, so the split cannot move — but that is reasoning, not a measurement. **T07** already carries the 47/72 check; this adds nothing new to it. |
| 2026-08-30 *(T05 review)* | 🐞 **Handing a parked revdiff back left its launch record saying `browse`.** `enterBrowse` sets that record on the way in, correctly; the untouched path relaunched nothing and never put it straight, so the next attach compared it against the agent's mode, mismatched, and quit and relaunched the pane. | The saving the park buys was thrown away on the first agent switch, taking the selected file, the scroll position and any unflushed annotations with it. Fixed in `leaveBrowse`. New section **11o** proves it, and failed against the unfixed code first. |
| 2026-08-30 *(T05 review)* | 📌 **Three restore paths orphan a live pane if `split-pane` fails** — the parked revdiff in `leaveBrowse`, the parked viewer in `enterBrowse`, and the incoming viewer in `showDiff`. Each drops its only reference, so nothing can reach or reap the pane again. | Left alone: the stub cannot make a split fail, so any fix would be untestable, and all three are wez-call failures. **T06** owns "no orphaned pane survives". |
| 2026-08-29 *(T05)* | 📌 **The test stub had `split-pane --move-pane-id` always landing in the cockpit tab.** True until now: parking the pair as a unit splits the viewer into its ALREADY-PARKED browser, so the moved pane must join the tab it is split into, not tab 0. | Fixed in `spikes/cockpit-test`'s stub, which is what makes "the two are parked together, in one tab" expressible at all. Any later test of a pane moved between two parked panes depends on it. |
| 2026-08-29 *(T05)* | 📌 **A parked pair means a THIRD pane per agent, and the revdiff is the one that hides.** `diffs` still means "the pane holding the slot" (so showDiff, the healer and the reaper are untouched); `browsePairs` and `parkedDiffs` hold whatever is out of it, each with the worktree it was launched in. | The cwd on both records is what rebuilds a browser rooted in a worktree the agent has left, and relaunches a revdiff whose range or tree moved while it sat parked. The reaper now kills all three. |
| 2026-08-29 *(T05)* | 📌 **The dragged-divider question T00 left open is answered by T05's own acceptance row**: the 47/72 split must come back after a round trip, so `--percent 60` is re-imposed on every restore. A divider the user drags inside browse snaps back on the next park. | Not invented here -- the task doc asks for exactly this. Nothing automated can drag one, so whether the snap-back is noticeable is **T07's** to judge, alongside the redraw. |
| 2026-08-29 *(T05)* | 📌 **That broot comes back with its FILTER TEXT is asserted nowhere.** The stub has no broot; T00 measured only that it "came back drawing its tree", and the filter box was measured surviving *pushes*, not a park. micro's tabs and cursor were measured surviving one. | The mechanism is the same untouched PTY, so it is expected rather than hoped -- but it is a claim no suite here makes. **One line for T07**: park, return, is the filter still there? |
| 2026-08-29 *(T05)* | 📌 **A half that has DIED rebuilds the whole pair on the next attach**, killing the healthy half with it -- and an orphaned viewer whose browser is gone is disposed of rather than left in the slot alone. | Deliberate for now: healing ONE half in place, without touching the other, is **T06** (DESIGN 2.n, "two halves, two independent heals"). This is the cruder fallback it will replace, not a second implementation of it. |
| 2026-08-29 *(T04 review)* | 🐞 **A worktree migration in browse mode took the keyboard.** `enterBrowse` always activated the browser, and `followWorktreeMigration` calls it -- so an agent creating a worktree mid-review would have yanked focus out of the Claude pane and put the rest of your sentence in broot's filter box. The revdiff branch beside it activates nothing, deliberately. | Fixed: `enterBrowse(..., {focus})`; the migration passes `diffPaneFocused()`, read before the old pair is destroyed, so focus **follows** the pair when it was already in the slot and is never **taken** when it was not. Asserted both ways, §11k. |
| 2026-08-29 *(T04 review)* | 🐞 **A failed `leaveBrowse` split left `panes.viewer` naming a killed pane.** The three keys were nulled only on the success path, but the anchor is disposed of either way -- so on a split failure the file kept advertising a dead viewer, and pane ids are reused. Exactly the hazard `disposeViewer`'s own comment names. | Fixed by unpublishing in the failure branch too. **Not asserted**: the stub has no way to make a `split-pane` fail, so this one rests on reading, not on a test. |
| 2026-08-29 *(T04 review)* | ⚠️ *(Superseded 2026-09-02 — the conclusion below does not hold on a real machine; see the top row.)* 📌 **Neither micro nor broot ever sets the terminal title**, so `BROWSE_TITLE` keeps matching after a push. Probed through `script(1)`: micro emits **zero** OSC sequences even with a file opened through its command bar; broot emits exactly one, `OSC 11;?` -- a background-colour *query*, not a title. | Closes the open risk in title-only detection: WezTerm falls back to the foreground process name only when the app sets none, and neither does. T06 can rely on the title through a whole browsing session, not just at t=1s. |
| 2026-08-29 *(T04 review)* | 📌 **A failed browser split leaves the mode inconsistent.** If `split-pane` fails, `enterBrowse` returns having already had the mode set to `browse` by its caller: the footer says Browse, revdiff is still in the slot, `reloadDiff` is suppressed and the healer only reports. It self-corrects on the next mode change. | Left alone under the scope rule -- reverting the mode means a return value threaded through four call sites, and healing a half-built pair is **T06**'s. Worth doing there rather than twice. |
| 2026-08-29 | 📌 **The pair is launched by TYPING into two spawned shells, not by spawning broot and micro as the panes' programs.** The task doc's interface shows `env … broot` on the split-pane line; a pane whose program *is* broot **closes** when broot is quit, turning T06's "heal a quit half" into "half the slot vanished". | T00 called this and it is now built that way: `spawnTerminal(worktree)` on the split (so `PATH` carries `cockpit-open`, `COCKPIT_REPO` the notes root), the command typed in after. DESIGN §3.2 is satisfied either way -- the env is still on the command line. |
| 2026-08-29 | 🐞 **The worktree watch would have typed `R` into broot.** `reloadDiff` fires on every agent write and sends revdiff's reload key to `diffs.get(jobId)` -- which in browse mode is the **browser**. broot has no reload: `R` is a character in its filter box. Found while building T04, guarded, asserted. | Nothing in the task doc predicted it; the watch belongs to the *agent*, not the mode, so it keeps running while you browse. The annotation watch is left alone deliberately -- the review file can only change when revdiff flushes, which cannot happen in browse mode. |
| 2026-08-29 | 📌 **A footer click column must be measured in UTF-8, and the capture kept OUT of the state dir.** The legend is full of multi-byte characters (`⌥ · ←↑↓→`); read as latin1 they count several times over and the click lands ~35 columns right of the label. And a capture file written *into* `COCKPIT_DIR` makes the renderer repaint on its own output for ever. | Both cost a confusing hour. `spikes/cockpit-test` §12 strips ANSI with node in utf8 and captures outside the watched directory. The click itself is real: SGR bytes down a `script(1)` pty, four labels, four verbs. |
| 2026-08-29 | 📌 **The click test runs a COPY of `cockpit-strip.mjs` under the run's own temp path.** Killing `script(1)` does not kill the node it spawned, so each click has to be cleaned up by name -- and `pkill -f cockpit-strip.mjs` matches the **footer of the live cockpit** (verified running from the main checkout, pids 71817/74133, while this suite ran). | Any later test that drives a cockpit binary through `script(1)` needs the same precaution. Four orphaned renderers per run is the alternative. |
| 2026-08-29 | 📌 **`spikes/cockpit-test` flakes at `COCKPIT_TEST_SPEED=1.0` too, given enough load.** One run in five failed -- in **section 1**, not the new work -- at a 1-minute load average of 44 (5-minute: 111), with the user's own cockpit and several agents running. Re-run at load 79: **205/205 green**. | Extends the existing 0.5 finding: the suite is timing-based end to end, so a red run is evidence about the machine until it is reproduced on a quiet one. Four of five runs green, plus four mutant runs that failed exactly where they should. |
| 2026-08-29 | 📌 **`CLAUDE.md` and `docs/cockpit.md` still say the diff has THREE modes.** True until T04, wrong now, and browse is not finished until T06 -- so the docs are deliberately left for the end of Phase 2 rather than updated three times. | Out of scope for T04 under the scope rule, and noted here so it is not lost. The same edit should fix the stale file map (six missing files, recorded above) and the suite's assertion count (134 → **205**). |
| 2026-08-29 | 🔄 **The user chose broot's own preview panel** for Enter on a non-text file, over doing nothing and over pushing it into micro anyway. Implemented as a second verb, `internal: panel_right`. Probed: a PNG draws a hex preview, an unreadable file draws `Permission denied` — neither leaves the terminal. | DESIGN §7 and the T03 task doc updated. The catch-all is `apply_to: file`, **not** `binary_file`. Whether a preview panel inside a 47-column browser reads well is T07's. |
| 2026-08-29 | 🐞 **The `--conf` chain was the wrong way round: the FIRST file wins, not the last.** broot takes the first verb matching a key across the whole chain — measured both ways round. T03 shipped the cockpit's file last, on the assumption "last wins". Found in review, fixed. | An `enter` the user bound themselves would have silently beaten the cockpit's, and the push would never have happened — and broot's own sample verbs.hjson invites exactly that swap. The user's config does not bind it **today**. `run.sh` **27**. |
| 2026-08-29 | 🐞 **`apply_to: binary_file` would not have plugged the hole.** An **unreadable** file is neither text nor binary: with all three kinds bound to separate recorders, only the general `file` verb fired for it. So the catch-all is `apply_to: file`. | An empty file counts as **text**; a broken symlink matches nothing at all (and `open` on a dangling link does nothing). Probed with every kind bound, so nothing could reach macOS during the probe. |
| 2026-08-29 | 📌 **broot resolves verbs by DECLARATION ORDER, not specificity.** With a general `apply_to: file` verb declared first, every text file matched it and the specific verb never ran. | So `cockpit-browse-verbs.hjson` must keep `text_file` **above** the catch-all — asserted, and the swapped mutant stops every push. It is also *why* chain position decides a key clash. |
| 2026-08-29 | 🐞 **broot IGNORES a verb field name it does not know.** `externol:` instead of `external:` loads perfectly clean, binds Enter and then does nothing — so the file is handed to macOS, the one failure the verb exists to prevent. Found in review. | "broot did not complain" is therefore not evidence the verb works, and every T03 check passed against that mutant. `run.sh` **26** presses Enter for real and is the only check that catches it. |
| 2026-08-29 | 📌 **`{line}` on a plain Enter is `0` — not empty, not absent.** Measured by pressing Enter against a real broot; the task doc, the verb file and the new tests all said "empty". No defect: `cockpit-open` already drops line 0. Found in review. | The three T03 checks asserted shapes broot never emits, while the real one was covered only by a pre-existing check. Comments corrected, that check relabelled. **T04 can rely on `<abs path> 0`.** |
| 2026-08-29 | 📌 **The whole verb is testable headlessly.** `script(1)` gives broot a terminal, a recorder named `cockpit-open` on PATH catches the argv: Enter fires, `{file}` is absolute, a `c/` search hands over the **matching** line, and Enter on a directory descends without pushing. | Moves three of T07's items from "a person must look" to automated. `run.sh` **26**, 223 → 228 checks, each proven to fail against an `apply_to: any`, a dropped `{line}` and a typo'd field name. |
| 2026-08-29 | 🐞 **Enter on a BINARY file falls through to macOS.** broot classifies a PNG as `binary_file` (probed without triggering it), so `apply_to: text_file` cannot match and broot's stock `open_stay` opens a GUI app over the terminal. | The plan mandates `text_file` and is right to — Enter must still descend into directories. But images and compiled artifacts sit in every repo. **Left as built; a decision for the user before T07.** |
| 2026-08-29 | 📌 **`broot --conf … --help` does NOT read the config** — a truncated file exits 0 with no complaint. broot parses (and validates) its verbs only on a real launch, so the suite launches it headless and reads its complaint. | Corrects the T03 task doc's suggested test. `spikes/browse-test/run.sh` now needs the **broot binary**; three controls (corrupt file, missing entry, bad `apply_to`) prove the check can fail. |
| 2026-08-29 | 📌 **A `--conf` entry that does not exist is FATAL**: `IO Error : No such file or directory`, and broot quits. A fresh machine has `conf.hjson` but no `verbs.hjson`. | `browseConfChain` names only files that exist, so browse mode opens on a machine that never configured broot. Naming all three unconditionally would have shipped a mode that never started. |
| 2026-08-29 | 📌 **Hjson unquoted values run to the END OF THE LINE**, so `{ key: enter, apply_to: text_file }` on one line is a parse error and broot then refuses to start at all. Cost one confusing probe. | Every field in `cockpit-browse-verbs.hjson` is on its own line, and the file says why. broot *does* validate the verb itself too — a bad `apply_to` or `key` is rejected at load, not ignored. |
| 2026-08-29 | 📌 **PATH cannot hide a Homebrew binary from `cockpit-layout.sh`** — it re-prepends `/opt/homebrew/bin` itself. So the guard test exports a bash `command` function (functions beat builtins and reach bash children) plus a `wezterm` function that records every call. | The guards are executed, not grepped, and a mutant with the `micro` guard deleted was checked to fail. `install.sh`'s missing branch is reached differently: point `SHELL` at a login shell that answers nothing. |
| 2026-08-29 | 📌 **CLAUDE.md's file map is stale**: it lists neither `cockpit-open.mjs`/`cockpit-open-model.mjs` nor any of the four agenda modules, so it is now missing six files. | Out of scope in T03, which added only its own two rows and the five→seven tool count. Worth fixing when T04 next edits that map. |
| 2026-08-29 | 📌 **`open.test.mjs` run standalone fails one check in §18** — with no `COCKPIT_DIR`, the agenda's default lock lands in the real `~/.claude/cockpit`. Through `run.sh` it passes. | Not a defect and not new; run the suites through `run.sh`. Cost a detour in T03. |
| 2026-08-29 | 🐞 **`viewer` was coerced, not checked.** `Number("")`, `Number(" ")`, `Number(false)` and `Number([])` are all **0** — a real pane, and on a fresh mux the *first* one. Probed: the push went out to `--pane-id 0`, exit 0. Found in review, fixed. | The one accident every refusal in `cockpit-open` exists to prevent. Accepted shapes are now named — a non-negative integer, or an all-digits string — not coerced. **T04 must publish `viewer` as a number or null**, never `""` or `false`. 11 new refusal checks. |
| 2026-08-29 | 🐞 **The `\r`/`\n` guard checked the argument, not what was sent.** A symlink resolving into a directory whose name holds a `\r` sent `open we\rird/f.js`; micro submits at the `\r` and runs `open we`. Probed before the fix. Found in review, fixed. | The resolved path is now checked too, next to the realpath that produces it. Exotic, but the guard's whole point is a byte that submits — and `realpath` is exactly where a path stops being the one you validated. |
| 2026-08-29 | 📌 **A failed send leaves micro's command bar OPEN**, and if the `open` triple landed before the failure the file is open but *unrecorded* — the next push of a different file `open`s over it, losing a tab silently. | The task doc chose this ("a failed send never updates the tab list") against the opposite risk, and the choice stands. **T07 should look at whether a failed push ought to type Esc** to close the bar it left open. |
| 2026-08-29 | 📌 **The lock now spans the sending**, so the critical section is up to six `wezterm` spawns against a **5 s** stale break. A send that hung past 5 s would let another push break a live lock. | Not reachable at ~50 ms a send, but it is why nothing slow may move inside `withLock(…, TABS_LOCK)`. The deviation itself is right: without it two simultaneous pushes both decide `open` and one buffer is lost. |
| 2026-08-29 | 📌 **`spikes/cockpit-test` flakes at its compressed default when the machine is loaded** — three runs, three *different* failing assertions (a click label, a focus jump, `[+ add]`), at load 4.5 with 21 claude processes. `COCKPIT_TEST_SPEED=1.0` (~119 s): **ALL PASS, zero failures.** | Re-run red at `COCKPIT_TEST_SPEED=1.0` before believing it, as `agenda-test` is re-run alone. The 0.5 default was swept on an idle machine. `cockpitd.mjs` imports no file T02 touched, so it cannot be reached from here. |
| 2026-08-29 | 📌 **The lock has to cover the SENDING, not only the read-modify-write.** Two pushes landing together both read an empty list, both decide `open`, and the second *replaces* the first's buffer instead of adding a tab — a lost tab, not a duplicate one. | `cockpit-open` holds `viewer-tabs.lock` across the whole transaction. Asserted with a stub that dawdles 80 ms per send: both land, one `open` and one `tab`. Deviation from the task doc, which locks only the state. |
| 2026-08-29 | 📌 **The payload goes on the `send-text` COMMAND LINE, not stdin** — the shape the T00 probe measured against a live micro. `cockpitd`'s own `sendRaw` pipes stdin instead, so the two look different on purpose; a stub that reads stdin (as `spikes/cockpit-test`'s does) would see empty payloads here. | T03's verb and T04's daemon work should not "unify" the two without re-measuring. `open.test.mjs` asserts the payload is the last argv element. |
| 2026-08-29 | 📌 **`withLock`'s reentrancy counter is now per lock FILE.** With one module-level number, a `viewer-tabs.lock` call nested inside an agenda one saw depth > 0, took the reentrant branch and ran holding **no lock on its own file** — the exact bug the counter was added to stop. | `bin/cockpit-agenda-store.mjs`: `Map<lockFile, depth>`. The agenda's own nesting is unchanged and `spikes/agenda-test` stays green. Asserted both ways, including that the nested call really creates its own lock file. |
| 2026-08-29 | 🐞 **`..` that climbed past `/` was carried upward, not clamped** — `/x/../../etc/hosts` → `/../etc/hosts`, and against a `repoRoot` of `/` it survived the prefix strip and came back as a `..` chain: the one label the module promises never to emit. Found in review, fixed. | The invariant is now structural, not true-of-the-cases-tried. Unreachable through broot (it hands back a resolved absolute path), which is why the tests missed it. `planPush` also owes an **absolute** `repoRoot`; `viewerRoot` (DESIGN §3.4) is one. |
| 2026-08-29 | 📌 **An empty or non-string `file` yields `open ` and files `""` in the tab list** — and a junk entry makes `openTabs` non-empty forever, so the real first push uses `tab` and micro keeps the dead `No name` buffer DESIGN §2.2 exists to prevent. The model has no refusal channel. | T02 rejects an empty path before calling, alongside the `\r`/`\n` guard. Reviewed and left in the model: degrading is right where the caller stat's anyway. |
| 2026-08-29 | 🐞 **`planPush` cannot `realpath` — it is the pure side (DESIGN §3.1) and `realpath` reads the filesystem.** So the model refuses the `../../..` chain and degrades to the **absolute** path instead; tested both ways. | **T02 must `realpath` BOTH `file` and `repoRoot` before calling**, or labels are absolute rather than wrong. Corrects the T00 finding's "T01's planPush must resolve both sides". |
| 2026-08-29 | 📌 **`line` must reach `planPush` as a NUMBER.** A string `"42"` off argv is not an integer, so the jump is silently dropped — the file opens, the cursor does not move. Asserted as a test, so it is visible rather than folklore. | T02 parses argv before calling. Dropping beats throwing: a bad line still leaves you the file. |
| 2026-08-29 | 📌 **Nothing quotes the path.** A filename with a space is sent bare (per the T01 doc); one holding `\r` or `\n` submits micro's command bar mid-path — unguarded, and out of T01's scope. | Whether micro accepts a bare space is **only checkable at a live pane — T07**. T02 guards: reject a path with `\r`/`\n` (`\r` is the worse one — it *submits*) or an empty one, where it already stat's. |
| 2026-08-29 | 📌 **T00 review re-ran all five probes from a clean checkout: 80 checks green, exit 0, no mux server or temp dir left behind, and an inherited `WEZTERM_UNIX_SOCKET` is ignored.** Two defects fixed — see the two rows below. | The spike is re-runnable evidence, not a transcript. `spikes/cockpit-test` and `spikes/notes-test` still green. |
| 2026-08-29 | 📌 **The pair in the slot holds up under every case the plan-review probe skipped.** Two agents both in browse (three parked panes each), the empty-slot rebuild, and a slot resize while parked: all return 120x15, browser 47 / viewer 72, tabs intact. | The architecture reversal is safe to build on. T05 has its dance measured. `spikes/browse-mode/`, 80 checks across five probes. |
| 2026-08-29 | 📌 **A pair swap is the slower of the two by roughly 6–11 ms** — single 17–26 ms, pair 25–31 ms, mean of twenty alternating swaps, four runs. At the original six reps the two overlapped and once *inverted*. | The two-parks cost is real but small; the exact figure is not defensible off one run. **Do not quote a delta from a six-rep run.** What the user sees is the repaint — T07. |
| 2026-08-29 | 🐞 **The 47 / 72 divider is re-imposed on every restore, not preserved.** Every restore ends with `split-pane --percent 60`, so the ratio is re-derived from a constant, which is *why* it survives a park, a switch and a resize. | **T05 decides**: a divider the user drags inside browse snaps back to 60 % on the next park/restore. The daemon would do the same. Not measured — nothing here drags one. RESULTS §1. |
| 2026-08-29 | 📌 `spikes/agenda-test/run.sh` returned **FAILURES once** while another worktree's session was running concurrently, then **ALL PASS three times** in isolation. Not caused by T00, which touched nothing outside `spikes/browse-mode/`. | Out of scope here, but T02 depends on that suite staying green: re-run it alone before believing a red. |
| 2026-08-29 | 🐞 **broot's `{file:path-from-directory}` returns an ABSOLUTE path, and relativising it needs `realpath` on BOTH sides.** broot resolves symlinks (`/private/var/…`), a worktree path usually does not (`/var/…`), and `relpath` between them yields `../../../../../../../private/var/…`. | **T01's `planPush` must resolve both sides and be tested on it**, or tab labels are worse than absolute. Corrects the T00 task doc, which assumed the verb relativised. RESULTS §4. |
| 2026-08-29 | 🐞 **A restored pane is a screen micro repainted INTO**; rows it does not touch keep the old content — blank if the pane grew, **stale file content if it shrank**. Both seen. | Reading a restored pane's screen with `head -1` is wrong: six spurious failures before it was spotted. Anything in the daemon reading a restored pane inherits this. `micro_tabbar`, `common.sh`. |
| 2026-08-29 | 📌 **broot draws no `│` frame either — 0 framed lines**, same blind spot as micro. So for both halves the pane **title** is the only signal `diffPaneStatus` can use. | T06 must detect the pair by title alone, and should tolerate a title lag rather than assume none (micro's is correct at t=1s; revdiff's is not). |
| 2026-08-29 | 📌 **Both halves have to be a shell with the command typed in, not a directly-spawned program** — DESIGN §2.n heals a quit half by relaunching in it, and a pane whose program *is* broot closes when broot quits. The probe uses the shell shape revdiff already uses. | Not a contradiction of §3.2 (the env still goes on the `split-pane` line), but T04 must not spawn `broot`/`micro` as the pane's program. |
| 2026-08-29 | 📌 **The live cockpit runs the MAIN checkout, not this branch.** `~/.claude/cockpit/config.lua` records `repo = /Users/jan.krolikowski/src/agentic-ide` and `~/.wezterm.lua` symlinks into it — verified. | This plan is built in a worktree, so re-opening WezTerm would verify `main`'s code. T07 repoints with `bin/install.sh` (`--check` first) and points back afterwards. PROGRESS, top. |
| 2026-08-29 | 🔄 **The diff slot CAN hold a pair, and the user chose it.** Probed on a headless mux: broot 47x18 + micro 72x18 in the slot, parked as a unit (`move-pane-to-new-tab`, then `split-pane --move-pane-id`), restored to **identical** geometry; micro still held its file with `[ro]`, broot still drew. | Reverses DESIGN §3.1 — the old "it breaks the park-exactly-one invariant" was asserted, never measured. Real cost: one extra `wezterm cli` call each way. Deletes the `browse` command entirely. |
| 2026-08-29 | 📌 **`--percent 60` to the viewer gives broot 47 columns** on a 120-column window — the exact width broot was already measured usable at during planning. | The split ratio is chosen to land on a known-good number, not guessed. Comfort is still T07's to confirm. |
| 2026-08-29 | 🐞 **`cockpit-notes.mjs`'s lock can write unguarded.** It waits 40 × 25 ms = 1 s against a 5 s stale window, then leaves the loop with `fd === null` and runs the write anyway. `cockpit-agenda-store.mjs` already fixes this (`LOCK_TRIES = ceil(STALE/WAIT)`). | Out of scope here — notes belong to another feature. Browse mode reuses the agenda's `withLock` rather than copying the broken one (T02). Worth its own task some day. |
| 2026-08-29 | 🐞 **A running micro reads as a bare `shell` to `diffPaneStatus`.** Measured on a headless mux: **0** lines start with `│` (the check needs ≥5) and the title is `micro`, which does not match `/revdiff/`. | `healQuitDiff` (1 s tick, 3 s cooldown) would type a whole command line into the live viewer. T06 fixes detection but lands two tasks after browse becomes reachable — see the plan review. |
| 2026-08-29 | 🐞 **Two `panes.json`/`terminals.json` fields do not mean what T02 assumed.** `panes.json.repo` is the **projects root** the cockpit was opened in (measured: `/Users/jan.krolikowski/src`), not a repo root; `terminals.json.agent` is the agent's **display name**, not a jobId. | The daemon now publishes `viewerAgent` (jobId) and `viewerRoot` (worktree) beside `viewer`. DESIGN §3.4. Without it, tab labels were wrong and no reap could ever drop a tab list. |
| 2026-08-29 | 📌 **revdiff's own browse still cannot do the two things browse mode is for.** `--dump-keys` on v1.12.0 shows only `f filter` (file list) and `/ search` (current file): no directory folding, no cross-file content search. `-A/--all-files` does exist. | Confirms DESIGN §7's broot+micro choice independently of the 6–12 s timing, which this review did not re-run. |
| 2026-08-29 | 📌 **Environment re-verified, every row of DESIGN §5 holds.** micro 2.0.15, broot 1.59.0, node v24.2.0, wezterm 20240203-110809-5046fc22, git 2.50.1, revdiff v1.12.0, macOS 26.5.1 (25F80), `timeout` still absent. `micro -readonly` and broot's `{file}`/`{line}` both exist. | Phase 1 and 2 build against a machine that still matches the plan. Seatbelts in §5.2 are real, not assumed. |
| 2026-08-29 | 📌 **The footer already needs ~184 columns** with three mode labels, a short agent name and no custom ref; a fourth label adds ~11, and `Custom: <branch>` adds more. | Whether `browse` gets a clickable footer label is a width decision, not a free one. DESIGN §2.7. |
| 2026-08-29 | ✅ **Verified by hand with the user:** on a real WezTerm fleet list, ⌥t *and* a click on `[+ add]` each open a shell, and it comes up in the agentic-ide checkout — not a worktree. | The stubbed suite drives the `cmd` verbs directly, so it can never prove the real key or pointer still delivers them. This is that half. |
| 2026-08-29 | 🐞 *(outside this plan — a direct fix)* **The fleet list's terminal gestures were dead.** `terminalCommand` bailed unless an agent was attached, so ⌥t/⌥w/⌥[/⌥], `[+ add]`, `[x]` and `select-<n>` did nothing there while the strip kept drawing them. | The list's shells are a normal terminal set; the guard bought nothing. Also: unattached, ⌥[/⌥] now always mean terminals — the top pane is the notes display, not a diff. Commit `terminal gestures work at the fleet list`. |
| 2026-08-29 | 📌 **Enter is `\r`; `\n` does nothing.** Sending `\n` to submit micro's command bar silently fails — no error, no tab, nothing. Cost a full failed run during planning. | The project's `\r`→`\n` rule applied in reverse: the cockpit strips `\r` so reviews arrive unsent, but here submission is the point. DESIGN §2.4. |
| 2026-08-29 | 📌 micro survives park/restore intact: `move-pane-to-new-tab` then `split-pane --move-pane-id` back returned **all 4 tabs**, cursor still on line 55, `[ro]` still set, geometry identical. Visible redraw on return. | Makes DESIGN §2.6 (park, don't kill) buildable. Still does not prove the slot holds *two* panes per agent — that is T00. |
| 2026-08-29 | 📌 **Focus is never stolen** by a push. The browser pane stayed active through four consecutive pushes and kept its filter text. | The whole gesture works without leaving broot. Assert it in T00. |
| 2026-08-29 | 📌 WezTerm titles a micro pane **`micro`**, stable from t=1s — no lag. revdiff's title lags ~1s, which is why `diffPaneStatus` needs two signals. | Viewer detection can be simpler than revdiff's, but T06 should still tolerate a lag rather than assume none. |
| 2026-08-29 | 📌 broot `--conf a;b;c` **layers** configs, it does not replace. Verified: the user's own `alt-o` and the cockpit's Enter verb both appeared in the same help table. | The cockpit ships its own verb file and never writes to `~/.config/broot/`. DESIGN §7. |
| 2026-08-29 | 📌 broot is comfortably usable at **47 columns** — the attached agent's terminal width. Tree, folders and `c/` snippets all readable. | Confirms the browser can live in the terminal slot (DESIGN §3.1). "Comfortable" is still a human judgement — T07. |
| 2026-08-29 | 📌 **`timeout(1)` does not exist on this machine.** Two planning probes died on `command not found`. | Spike scripts must background-and-kill, never `timeout`. DESIGN §5. |
| 2026-08-29 | 📌 micro started with no file leaves a permanent empty `No name` first tab. | First push uses `open`, later ones `tab`. DESIGN §2.2. |
| 2026-08-29 | 📌 Re-pushing an open file creates a **duplicate tab**; micro cannot be asked what it has open. | The glue must remember what it sent and use `tabswitch <n>`. DESIGN §2.5. |
| 2026-08-29 | 📌 macOS reserves **ctrl-← / ctrl-→** for Mission Control desktop switching (verified enabled on this machine), so broot's own preview keys never reach the terminal. `⌥p`/`⌥o` were bound in the user's `verbs.hjson` instead. | Not part of this plan — already done — but any doc telling the user to press ctrl-→ is wrong on this machine. |
| 2026-08-29 | 📌 broot's `:panel_right` shows **only the matching lines with real line numbers** ("lines: 3/467") when a `c/` search is active; `:toggle_preview` opens the file at line 1 and loses that. | The cockpit's verb layer must use `:panel_right`. The difference is the entire value of the content search. |
| 2026-08-29 | 📌 `ff` and `fp` in `~/.claude/cockpit/bin` are **dangling symlinks** into a deleted job directory — they are broken commands in every cockpit terminal today. Durable source is in `ideas/terminal-find-in-files.md`. | Out of scope here (DESIGN §8), but noticed and left alone under the scope rule. The user has not decided whether broot replaces them. |
