# Findings log

**What the build taught, especially where reality contradicted the design.**

**Read this before touching a task**, and read it whole before anything that only a person can
verify. It is the only place a hand-verification is written down: the test command is the only
evidence a session can produce on its own, so a ✅ line here is the *entire* record that
something was seen working for real.

**Newest first. About forty words a row.** The long version belongs in the commit message that
carried the fix; this is the index, not the account.

Legend: 🐞 a defect found · ✅ verified by hand with the user · 📌 worth knowing ·
🔄 a decision the user changed.

| Date | Finding | Consequence |
|---|---|---|
| 2026-08-28 | 🐞 **Three ways a line escaped `renderAgenda`'s own contract**, found in T03 review: the **header was never clipped** (`pad` only grows, so under ~11 columns it overran `width`); **two rows could both wear `NOW`** (matched by event id, and Google reuses one id across calendars); an **event title could inject control sequences** (a newline painted a 7th terminal line out of 6 array entries). | All fixed, 288 → **300**. **Untrusted text is now sanitised where it enters the drawing** — T04/T05 need not re-strip titles, but must not undo it. The width sweep now starts at 1, not 12. |
| 2026-08-28 | 📌 **`cockpit-welcome.mjs` still has its OWN `clip`, and it behaves differently** — it does a raw `s.slice()`, which counts escape bytes as columns and can cut an escape in half. The model's exported one walks the string. T03 exported the helpers but left the pane's copies, correctly, as T06's work. | **T06's swap is a behaviour change, not a move**: notes carrying escapes will clip differently and better. Expect `notes-test` (39) to stay green, but read it rather than assuming. |
| 2026-08-28 | 🔄 **The scope line says `TODAY · Wed 26 Aug`, both scopes labelled** — the user's call, over the bare date the T03 and T06 task docs both draw. DESIGN §2.3 already said this and now stands unamended. | **T06's mock-up in its own task doc is out of date**: follow DESIGN §2.3 and the rendered output, not that block. The word is what changes meaning, so it is drawn bright and the date beside it dim. |
| 2026-08-28 | 📌 **T03 fixed the cache's error contract, and T04 has to write it.** `error` is a bare string or `{ kind }` — `network` \| `auth` \| `gone` \| `unknown`. Scope-insufficiency travels as kind `auth` with `reason`/`code`/`detail` naming SCOPE (or kind `scope`). | `auth`/`gone` are loud and drop that calendar's rows; **`network` AND `unknown` are both quiet** — a malformed 200 is nothing the user can act on, and §2.7 is loud only when a command fixes it. An unrecognised kind reads as transient. |
| 2026-08-28 | 📌 **`renderAgenda` and `agendaHeight` take `tz`, which the task doc's interface omitted.** Without it the column draws in UTC on a Warsaw machine; reading `Intl`'s default zone inside the model is exactly the environment read §3.1 forbids and the purity grep cannot see. | **T05 and T06 must pass the machine's zone** from the impure side, beside `now`. It defaults to UTC, so forgetting it is a wrong column rather than a crash — nothing will fail a test for you. |
| 2026-08-27 | 🐞 **`normaliseEvent` leaked two ways.** A `responseStatus` naming an Object property (`constructor`, `toString`) walked the prototype and returned a **function** as `reply`; a `dateTime` with **no offset** was parsed in the **machine's** zone — the §3.1 read the purity grep cannot see, because it happens inside `Date.parse`. | Both fixed in T02 review, +6 assertions (157 → **163**). An offset-less stamp is now placed by its own `start.timeZone`, else the calendar's `tz`. **T03 may trust `reply` is one of the five strings.** |
| 2026-08-27 | 📌 **`chooseEvents(events, now)` throws `RangeError` when `now` is not a real epoch** — `undefined`, `NaN` or a string all reach `Intl` as an invalid date. (`null` silently means 1970.) It is a caller contract, not data, so the model deliberately does not guess. | **T03/T06 must hand it `Date.now()` from the impure side**, never a number read out of a state file or a cache. The failure mode is a thrown renderer and a blank pane, which §2.7 says must not happen. |
| 2026-08-27 | 📌 **`chooseEvents` takes calendar-add order from the ORDER OF ITS INPUT ARRAY.** All-day rows have no start time to sort by, and the signature carries no calendar list, so nothing else can rank them. The timed sort is by start alone and relies on `Array.sort` being stable. | **T04 and T06 must concatenate each calendar's events in `state.calendars` order** before calling it. Get that wrong and the all-day rows shuffle, and two 17:30s swap places between refreshes. |
| 2026-08-27 | 📌 **A day is not always 24 hours, and an all-day event is a civil date, not an instant.** Warsaw's 25 Oct 2026 runs 25 hours; midnight+24h lands an hour early and throws that evening onto the wrong day. The model resolves each midnight with the offset in force *at the answer*, two passes. | `dayBounds(now, { tz })` is exported for exactly this and returns `todayStart`/`tomorrowStart`/`dayAfterStart`. **T04's fetch window (§2.5) must use it**, never `now ± 24h`. |
| 2026-08-27 | 📌 **DESIGN §3.1's purity grep is now armed and was proven to bite**: adding a filesystem import and a clock read to `cockpit-agenda-model.mjs` turned `agenda-test` red, removing them turned it green. `run.sh` needed no edit — it globs `*.test.mjs` and self-arms on the file appearing. | **T03 extends the same file and inherits the check.** If it ever fails the fix is to move the code out, never to relax the grep. agenda-test is now **157** assertions. |
| 2026-08-27 | 🐞 **`withLock` was not reentrant, and one lock over three files makes nesting ordinary.** A compound write (add a calendar, prime its cache) took **5035ms** and the inner call broke the outer's own lock as stale and unlinked it — the rest of the transaction ran unlocked. | Fixed in T01 review by depth-counting; the test asserts the lock is still held after the inner calls, not just that it was fast. **T04/T05/T07 may now wrap several store writes in one `withLock`** — that is what it is for. |
| 2026-08-27 | 📌 **Two guards in `agenda-test/run.sh` read stronger than they were.** The real-`~/.claude/cockpit` check compared filenames only; the zero-dependency grep missed single quotes and `await import()` and was matching prose in a comment. | Both hardened in T01 review. The real-dir check now stats `agenda*` (name+size+mtime, never contents) — **not the whole directory, because a live cockpit rewrites `terminals.json` constantly** and would fail the suite spuriously. agenda-test is now **65** assertions, not 60. |
| 2026-08-27 | 📌 **`spikes/agenda-test/run.sh` exists and is the plan's test command; baseline 60 assertions.** It runs every `*.test.mjs` beside it in its own fresh `COCKPIT_DIR`, then checks in bash that nothing reached the repo or the real `~/.claude/cockpit`. | Later tasks add a suite file rather than editing `run.sh`. **DESIGN §3.1's purity grep is written but skipped** — it prints `the pure model does not exist yet` until T02 creates `cockpit-agenda-model.mjs`, then arms itself. T02 must see it turn green. |
| 2026-08-27 | 🔄 **`agenda setup` takes the path to Google's downloaded JSON**, not two typed values — the user's decision once T00 showed the real file is nested. | DESIGN §2.9 and §7 amended. The parse is a pure `parseGoogleClient(text)` in the model, added and tested by **T05** — **supersedes** the row below routing it to T04. T01's store stays dumb and receives normalised values. |
| 2026-08-27 | 📌 **A 403 carrying `ACCESS_TOKEN_SCOPE_INSUFFICIENT` must classify as `auth`, not `gone`.** The consent checkbox finding below has a behavioural consequence the plan had wrong. | DESIGN §2.7 amended; T04 gains the rule and two tests. Rendering `calendar gone · agenda rm` for a consent mistake would destroy a working configuration and fix nothing. |
| 2026-08-27 | 📌 **The cockpit-test baseline moved again**, 108 → 117, from two footer/click-diff-mode commits landing on `main` (`0562a6a`, `8339211`). Re-measured on `8339211`: notes-test 39, cockpit-test 117, both ALL PASS. | Third time this number has drifted mid-plan. **Re-measure it at the start of any task that touches `cockpitd.mjs` rather than trusting the recorded figure** — the rule that matters is ALL PASS with no count going down. |
| 2026-08-27 | ✅ **Both Google accounts connect.** The personal and the company account each completed the loopback+PKCE flow by hand and returned their calendar lists; both issued a refresh token. Neither was blocked by an admin. | The risk that gated the whole plan (§7, T00) is retired — no private-iCal fallback is needed. T01+ may proceed on the assumption both accounts work. |
| 2026-08-27 | 📌 **Publishing status *Testing* works for connecting and issues refresh tokens.** The ~7-day refresh-token expiry in *Testing* (DESIGN §2.9, still 📌 below) is **not** settled — it needs a week of elapsed real time to see. | Partially supersedes the "believed, not verified" row: *Testing* is confirmed usable now. If tokens do expire weekly, §2.7's loud `sign-in expired · agenda add work` line already covers it — no redesign, which is why this stays a risk, not a blocker. |
| 2026-08-27 | 📌 **Two console gotchas, both user-action not code:** (a) an account not on the OAuth screen's **Test users** list gets a hard "Access blocked — not verified" with no way past; it must be added. (b) Google's **granular consent** shows a per-scope checkbox — leaving the calendar box unticked yields a token that 403s `calendarList` with `ACCESS_TOKEN_SCOPE_INSUFFICIENT`. | T04 and `agenda setup` docs must tell the user to add every account as a test user and to tick the calendar permission. The 403 is indistinguishable from a scope bug but is a consent choice. |
| 2026-08-27 | 🐞 **Google's downloaded Desktop-client JSON is nested**, not flat: `{ "installed": { client_id, client_secret, … } }` (snake_case). The probe first assumed the task doc's flat `{ clientId, clientSecret }` and rejected the real file. | Probe now reads `installed`/`web`/flat and both key styles. **T04's store/client (`agenda-client.json`, DESIGN §3.5) must accept the nested shape too**, or paste-the-file-Google-gave-you (§2.9) fails the same way. |
| 2026-08-26 | 📌 **The plan was written against `c9e4592`; six commits landed on `origin/main` mid-session** (reflog-watch diff reload, parked-worktree following, a Cmd+C change). `cockpitd.mjs` grew ~185 lines and `cockpit-test` grew 82 → 108 assertions. | T07's hook was re-checked against the new code: `onExit()` still exists and is still called from `reconcile()` on the list transition. Baselines above are re-measured, not inherited. |
| 2026-08-26 | 📌 **Refresh-token expiry is believed, not verified.** A Google client left in *Testing* status is thought to expire refresh tokens after ~7 days; *In production* is thought to fix it, at the cost of an "unverified app" warning. From memory, not measured. | T00 must confirm which publishing status works and record it here. If weekly re-auth is unavoidable, DESIGN §2.7's loud `sign-in expired` line already covers it — no redesign. |
| 2026-08-26 | 📌 **Google's OAuth endpoints verified live** from `accounts.google.com/.well-known/openid-configuration`: auth `.../o/oauth2/v2/auth`, token `oauth2.googleapis.com/token`, revoke `.../revoke`, and `S256` is in `code_challenge_methods_supported`. | PKCE loopback is confirmed available. T04 hard-codes these rather than fetching discovery at runtime. |
| 2026-08-26 | 📌 **Node v24.2.0 has everything needed built in** — `fetch`, `node:http.createServer`, `webcrypto.subtle.digest`, `crypto.randomBytes` — all confirmed on this machine. `Intl` reports `Europe/Warsaw`. | The zero-dependency rule (DESIGN §5) holds. No task should reach for a library; if one seems to, that is a question for the user. |
| 2026-08-26 | 📌 **`/usr/bin/cal` exists and the cockpit *prepends* its bin dir to PATH** (`bin/cockpit-layout.sh:77`), so a `cal` symlink would shadow the month grid in every cockpit terminal **and in every agent**, which inherits that PATH. | 🔄 The user renamed the command to `agenda`. DESIGN §2.2. |
| 2026-08-26 | 📌 **Baseline test measurement**: `spikes/notes-test/run.sh` 39 assertions ALL PASS, `spikes/cockpit-test/run.sh` 117 assertions ALL PASS, on a clean `main` at `8339211`. | Any session that reddens either has broken something it did not mean to touch. Both are part of the test command (DESIGN §5). |
| 2026-08-26 | 📌 **`cockpitd.mjs` already detects the return to the fleet list** — `onExit()`, called from `reconcile()` on the list→agent transition, at an 800ms poll. | T07's "refresh when I come back" hook exists; it does not need a new detector. |
