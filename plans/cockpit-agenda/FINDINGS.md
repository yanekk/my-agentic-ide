# Findings log

**What the build taught, especially where reality contradicted the design.**

**Read this before touching a task**, and read it whole before anything that only a person can
verify. It is the only place a hand-verification is written down: the test command is the only
evidence a session can produce on its own, so a ✅ line here is the *entire* record that
something was seen working for real.

**Newest first. About forty words a row.** The long version belongs in the commit message that
carried the fix; this is the index, not the account.

Legend: 🐞 a defect found · ✅ verified by hand with the user · 📌 worth knowing ·
🔄 a decision the user changed.

| Date | Finding | Consequence |
|---|---|---|
| 2026-08-29 | ✅ **The `NOW` row is right on the real screen, against the real clock.** Screenshot at **17:04**: `AGENDA` with the clock right-aligned, `TODAY · Sat 29 Aug`, then `▌ NOW   EVENT` with the slug `home` right-aligned and `└ until 18:00` beneath — a violet bar for the violet calendar. The event had been created minutes earlier and **arrived on its own**. | **T08 block 2 is closed, and block 3 with it**: the pinned-now event, its `└ until HH:MM`, the scope line, the right-aligned clock and slug, and the refresh picking up a brand-new event unattended, all on the live pane. **Precisely**: only `home` had an event, so green and violet were **not** seen side by side — the palette-legibility question was already closed by T06's ✅ with teal/amber, and is not re-opened by this. |
| 2026-08-29 | ✅ **A second calendar from an account already signed in opens NO browser — seen by hand.** `agenda add holidays` offered the account menu (the known address, then `a different account (opens a browser)`), and picking the known one went straight to the calendar picker — three calendars, the user's own marked `(your own)`. Attached green; a later `agenda add home` attached violet from the same account. Both fetched, 0 events. | **DESIGN §2.1's "reuses the stored sign-in and opens no browser at all" is verified.** It is the reason `accounts` and `calendars` are separate tables. This is the substitute for the deferred `agenda add work`, and it covers the *flow's* ergonomics — picker, menus, the numeric re-prompt on a blank answer — which FINDINGS 2026-08-28 routed to T08. The company account's own `add` stays deferred. |
| 2026-08-29 | 📌 **A calendar vanished mid-session and it was NOT a defect — the user had removed it.** Chased before asking: the add path is covered by a *passing* test (`add work`, then `add home` picking the **known** account, asserts `ls` prints two lines); the daemon never writes `agenda.json`; the layout script only symlinks; and the account survived with its original `addedAt` and no quarantine file — so only `agenda rm` could have done it. | **No fix, and none needed** — but the elimination is worth keeping: `agenda add`/`rm`/`color` are the **only** writers of `agenda.json`, which is what makes "a calendar disappeared" answerable at all. Consequence for T08: **both calendars now sit on ONE account**, so block 5 cannot show a revocation sparing a *different* calendar. That half travels with the work account to the end of the plan. |
| 2026-08-29 | 🔄 **The work account cannot be used on this machine — the user's call, taken in T08. That check moves to the END of the plan.** T08's "Needs a person" block 1 asked for `agenda add work`; the second account is unavailable here, so it is deferred rather than guessed at. | **T08 does not close the company-account half.** DESIGN §5.1's "whether the **company** account may connect at all" row was already answered ✅ 2026-08-27 by T00's probe, so what is deferred is the narrower question of `agenda add` against it. Substituted: a **second calendar from the account already signed in**, which yields the two-calendar column the later blocks need *and* exercises DESIGN §2.1's no-second-browser path, which nothing had verified. |
| 2026-08-29 | 🐞 **The "start over" recovery command deleted the registration the same sentence told you to spare.** DESIGN §6 said `rm ~/.claude/cockpit/agenda*.json`; the glob takes `agenda-client.json` too. Somebody following it under pressure loses the Google console setup as well as the sign-ins. | **Fixed, the user's decision** (T08). §6 now leads with the two-file command and keeps the glob as a separate, labelled "including the registration" line. Verified in a temp `COCKPIT_DIR`: the registration survives and `agenda` falls back to `no calendars`. |
| 2026-08-29 | 🐞 **`agenda rm` and `agenda color` were refused with a reason that is untrue of them** — one shared message told all four verbs "It would open a browser and wait for a person to finish signing in". Neither opens one; the code comment already knew the real reason ("a configuration change nobody asked for"). | **Fixed** (T08): the reason is now chosen per verb. Four assertions added pinning the browser sentence to `add`/`setup` only, so the wrong reason cannot come back. agenda-test 585 → **589**. Found by running the recovery steps, not by reading. |
| 2026-08-29 | 📌 **`notes-test` section 11 is load-sensitive and flakes.** "A replaced cache is on screen without a restart" gives the watch **1200ms** while the fallback repaint sits 2s out; under load the child pane misses it. Seen **once in five runs** in T08, with `cockpit-welcome.mjs` and `notes-test/run.sh` both untouched. | **Logged, not fixed** — scope. It is the same class as the `cockpit-test` load-sensitive spots already noted. The margin is the fix (the deadline only has to stay under the 2s fallback to still prove the watch), but it belongs to whoever owns that suite, not to T08. **A single red run here is not evidence of a regression.** |
| 2026-08-29 | 📌 **The one-minute tick and the start-up refresh were both seen on the real machine** — read out of the live `~/.claude/cockpit/daemon.log` during T07's review, on the *current* build: `agenda start: home ok` at 10:54:42Z, then **fourteen consecutive ticks, 60.0s apart**, to 11:08:42Z. Re-checked independently: **0** hits for `ya29`/`bearer`/`refresh_token`/`access_token`/`client_secret`/`GOCSPX`. | The **boot call the user approved is evidenced live**, and so is the 60s window — the ✅ row below was gathered while the window was still five minutes, so it did not cover either. **Not a hand-verification**: a log read, not a screen, and the cache held 0 events at the time so the title check proved nothing new. |
| 2026-08-29 | 🔄 **The third call site STAYS — the user's decision, taken in T07's review.** `refreshAgenda("start")` at boot is required because `onExit` does not fire at window-open, and DESIGN §2.5 counts opening the window as a return. | **T07's "Done when" is amended in place** on its own task sheet, which now also carries a banner pointing at DESIGN for the one-minute number. The sheet stays otherwise unedited: it is the record of what was asked for, not of what was built. |
| 2026-08-29 | 📌 **At a 60-second tick the ON-RETURN trigger can almost never fire.** The live log shows **11 returns to the fleet list and zero `agenda returned:` lines**: the tick keeps every calendar under 60s old, so the staleness predicate suppresses on-return every time (closest miss 56s). | On-return is now a **safety net, not a path anyone travels** — only cockpit-test 11b, whose tick is an hour, exercises its fetching half. **Keep it** (it covers a stalled or slowed tick) and do **not** "fix" it by bypassing the predicate: that would fetch on every agent hop, which is exactly what the predicate exists to stop. |
| 2026-08-29 | 📌 **A permanently broken calendar is retried every 60 seconds, for ever, with no backoff.** A failure deliberately keeps the previous `fetchedAt` (T07 deviation 4), so the calendar never stops reading as stale. | ~1,440 failed token requests a day per dead calendar until the user runs `agenda add` — five times what it was before the window went to 60s. **No backoff is specified anywhere in the plan and none was invented in review.** If Google ever rate-limits this client, this is the first place to look. |
| 2026-08-29 | 📌 **Two daemon edges found in review and deliberately left alone.** A **backwards clock step** freezes all refreshing for as long as the step (`now - fetchedAt` goes negative, so nothing reads as stale). A **hand-edited `agenda.json` with slug `__proto__`** makes `putCacheEntry` write to the object's own prototype, so that calendar's cache write vanishes and it re-fetches every tick for ever. | Neither crashes anything; neither is reachable without a manual edit or a manual clock change. The slug contract belongs to **T05** (`agenda add` already refuses `__proto__`), and DESIGN §2.7's clock paragraph covers the *rendering*, not the staleness predicate. |
| 2026-08-29 | 📌 **T07's mutation sweep was re-run in review with three mutations the build session had not tried**: `now ± 24h` instead of `dayBounds`; the event title appended to the log line; the staleness predicate dropped. A fourth bumped `fetchedAt` on failure. Each turned **exactly its own assertions red and nothing else** (2 + 1 + 2 + 2). | Section 11 is proven **nine ways** now, not five. Note the shape of the fourth: nothing asserts the kept `fetchedAt` *directly* — it is defended because a bumped one leaves the calendar fresh, so the auth failure that follows is never attempted. Re-run the sweep after any edit to `refreshAgenda`. |
| 2026-08-29 | 📌 **cockpit-test has more than one load-sensitive spot, and the terminal-close assertions are another.** Under a load average of ~12 (suites back to back plus four live agents) `[x] on the on-screen terminal closed it` and `one terminal left after closing #2` both failed, in a run whose mutation could not possibly reach them. | **Same rule as the §9d flake below: do not chase these as a regression.** Re-run on an idle machine before believing a red in the terminal or cwd-migration sections. Only assertions that fail *and* name the thing you changed are evidence. |
| 2026-08-29 | 🔄 **The refresh window is ONE MINUTE, not five — the user's call, made after feeling the cost.** Watching the live cockpit they added an event and waited ~4.5 minutes for it: a refresh had run at 10:35:43, so the data was "fresh" and the daemon correctly did nothing until 10:40:43. The wait for a newly created event was **0 to ~6 minutes**, and worst exactly when you are watching for it. | **DESIGN §2.5 amended** (and §2.7's staleness sentence). `AGENDA_STALE_MS` 5min → **60s**, so window and tick coincide and it is now a once-a-minute poll — a few thousand small calls a day against a quota in the millions. **The staleness predicate is kept, not dropped**: it is what stops the *on-return* trigger re-fetching on every hop between agents. **T07's task doc still says `5 * 60_000` and is out of date** — follow DESIGN. Guard in cockpit-test 11c re-pinned to the new default. |
| 2026-08-29 | ✅ **The daemon was seen refreshing a real Google calendar, on its own, in a live cockpit.** Two consecutive ticks: `10:35:43 home ok, 0 events` then `10:40:43 home ok, 1 events` — 300.1s apart, each firing on the first tick past the five-minute boundary rather than on a timer of its own. The second picked up an event the user created ~3 minutes earlier, with **nobody touching the cockpit**. The user confirmed the column filled itself in. | **T07's hands-on half is closed.** Also measured on the same log: **0** matches for `ya29`/`bearer`/`refresh_token`/`access_token`/`client_secret`/`GOCSPX`, and the cached event's 15-character title appears **nowhere** in `daemon.log` — checked by comparing the cache against the log, so it is a real event's real title, not a fixture's. The on-return trigger was also exercised (`exit → fleet list`, 10:31) and correctly stayed **silent**: the data was one minute old. **T08 need not re-ask any of this** — it is left with the sign-in flow's own ergonomics and the docs. |
| 2026-08-29 | 📌 **`onExit` does NOT fire when you open the window**, though DESIGN §2.5 leans on it for exactly that case ("you opened the window, or you stepped out of an agent"). It only runs when an agent *was* attached, and at start-up none is. | T07 therefore calls `refreshAgenda("start")` once at boot — a **third call site its own "Done when" forbids** ("from the tick and `onExit`, and nowhere else"). Without it the column sits blank for a minute after every cockpit rebuild. Raised with the user; overruling it is one line. |
| 2026-08-29 | 📌 **The 40 new assertions were checked by breaking the daemon five ways**: dropping the one-pass-in-flight guard, blanking events on failure, resetting `error.since` every tick, removing the on-return call, and letting the daemon rescue a corrupt `agenda.json`. Each mutation turned its own assertion red. | The section tests **behaviour, not "it ran"**. Re-run that sweep after changing `refreshAgenda`: a green suite here is worth only what the last mutation proved. `cockpit-test` is now **174**, not 134. |
| 2026-08-29 | 📌 **A stub launched as a FILE numbers its arguments from 2, not 1.** The fake Google read `argv[1]` as its mode file — that is the script's own path — so it never switched mode and appended its hit log elsewhere. Every failure mode silently returned **success**. | Caught only because the assertions check *outcomes* (an error kind in the cache), not "it did not crash". **A stub that cannot fail is worse than no stub**: every happy-path assertion still passes. |
| 2026-08-29 | 📌 **The daemon is the first LONG-LIVED process to take the store's lock, and that lock waits with `Atomics.wait`** — it blocks the thread. The store's own comment assumes a caller that "prints and exits". Normal contention is ~1ms; a lock left by a *killed* process is only broken after 5s. | So a crashed `agenda` could freeze the whole cockpit — reconcile, the `cmd` channel, the quit-diff healer — for up to five seconds, once, then heal. **Out of T07's scope and left alone.** The fix, if it ever bites, is an async lock in the store — never a second copy of the write inside `cockpitd`. |
| 2026-08-29 | 📌 **Two extra daemons, not one, is what makes "tick" and "on-return" separable.** The wezterm stub takes every path it uses from the environment, so a second and third `cockpitd` get their own pane table and state dir for the price of an env prefix. D3's tick is an hour long, so anything it refreshes is *provably* the return trigger. | Reusable for anything in `cockpitd` needing a second instance. **`AGENDA_STALE_MS` is 60s in both** — longer than the section runs — so nothing is re-fetched by accident and every fetch is one a line asked for. |
| 2026-08-29 | 📌 **The T06 preview fixture is already gone.** `~/.claude/cockpit/` holds no `agenda*.json` at all, and **no calendars and no accounts are configured on this machine.** | PROGRESS's "clear it before T07" instruction is **discharged, and nothing was deleted**. It also means T07's hands-on half cannot run until a real `agenda setup` + `agenda add` — the sign-in flow **T08** owns and no session has yet seen work. |
| 2026-08-29 | 🐞 **The resting pane was rescuing a corrupt `agenda.json`, and rescuing it silently.** `readState()` quarantines the file; the CLI reads `corruptedTo` and says what it moved and that the calendars need adding again. The pane repaints every 2s, so it always won that race and had nobody to tell — DESIGN §2.7 makes the announcement the point of the rule. | Fixed in T06 review: `readState({ rescue: false })` reads without moving anything, default unchanged. 579 → **585**, 88 → **90**. **T07's daemon must pass it too** — same shape, continuous and mute. Only the `agenda` command may rescue. |
| 2026-08-29 | 📌 **A second plan's files rode into the T06 hand-verification commit.** `2171822` carries four `plans/browse-mode/` files alongside its two `cockpit-agenda` edits; the rest of that plan was then committed properly on its own in `16a70a9`. | Nothing is broken and history is **left alone** — folding it is the user's call (CLAUDE.md). Named here only so a later session does not read `browse-mode` as something `cockpit-agenda` depends on. It is a separate, unrelated plan. |
| 2026-08-29 | ✅ **The column was seen on the real screen, and it is right.** After a cockpit rebuild the user confirmed both things no test can reach: the divider between the greeting and the right column **lines up down the whole pane** — no jog where the rule meets it — and the two calendars' colours (teal, amber) are **distinguishable from each other and readable** on their terminal theme. Driven by a throwaway preview fixture, no Google sign-in. | **T06's screen half is closed**, and DESIGN 2.8's palette claim is no longer an assumption on this machine. **T08 need not re-ask either question** — it is left with the sign-in flow and the refreshes, which are still unseen. The reviewer cannot re-run this; this row is the whole record. |
| 2026-08-29 | 📌 **The welcome pane writes nothing at all** — corrected in T06 review. It briefly did: `readState` quarantines a corrupt `agenda.json`, so the resting screen was moving the sign-ins aside. It now reads with `rescue: false`, and rescuing is the `agenda` command's alone. | **The pane is pure display again**, and notes-test §10 greps it: no child process, no socket, no `wezterm`, and no import beyond `node:fs` and the three cockpit modules. **T07 must not widen that list** — cockpitd owns this pane as the diff slot on exactly that basis. |
| 2026-08-29 | 📌 **The frame harness now freezes the clock and fixes `TZ`**, so `NOW`, `└ until 15:00`, `TODAY · Wed 26 Aug` and the wall clock are one string on every machine at any hour. Its escape strip also had no `;` in its class, so a calendar's `38;5;37` bar survived it. Both fixed in `spikes/notes-test/run.sh`. | The seam is in the **harness**, not the pane — the pane still reads `Date.now()` once per paint and has no test hook. **T08 may drive the pane this way**; a real clock makes an event three hours out cross midnight for anyone running the suite in the evening. notes-test is now **88**, not 39. |
| 2026-08-28 | 🐞 **Two defects in T05, both in lines the model never draws.** A Google calendar title reached the terminal raw — an ESC retitled the window and a newline forged a second `agenda ls` row. And `agenda add __proto__` attached a calendar whose cache write silently vanished (it set the object's prototype), after which bare `agenda` died on `entry.events.length`. | Both fixed in T05 review, 562 → **579**. The model's `safe()` is now **exported as `safeText`** and the CLI runs every wire-sourced string through it. **T06 and T07 must do the same** — a title, an email and Google's error text are all untrusted, and `strip()` removes only colour. |
| 2026-08-28 | 📌 **`cockpit-test` section 9d flaked once**, on the first of five runs, when the three suites ran back to back under the sandbox: "revdiff came back on the new worktree". Four later runs (two sandboxed, two plain) were 134 ALL PASS. Timing-sensitive, not a regression. | **T07 must not chase this as its own breakage.** Re-run before believing a red 9d, and treat one failure on a loaded machine as noise. Everything else in that suite was stable across all five runs. |
| 2026-08-28 | 📌 **Nothing tells the user about Google's two console traps at the moment they matter.** `agenda setup` explains the Desktop-app download but never says to add each account as a **test user**, nor to **tick the calendar checkbox** on the granular-consent screen — the 403 that FINDINGS 2026-08-27 measured. | Out of T05's scope (not in its doc or DESIGN §2.9) and **left alone**. **Routed to T08**, whose deliverable is the docs. The column already says `calendar permission not granted` loudly, so this is guidance, not a hole. |
| 2026-08-28 | ✅ **`agenda` is published inside the cockpit and nowhere else — seen by hand.** In a live cockpit terminal `agenda help` printed the usage and bare `agenda` drew `AGENDA / no calendars / agenda add home`; in an ordinary terminal `agenda` was `command not found`. Symlink only, no rebuild needed. | T05's last "Done when" is met, and the publication mechanism is no longer an assumption. **T06 and T07 may rely on the command existing in every cockpit shell** — including the agents', which inherit that PATH. |
| 2026-08-28 | 📌 **The CLI has three test-only env seams, and each one has a guard in `run.sh`.** `AGENDA_ORIGIN` (loopback stub), `AGENDA_BROWSER` (the fake browser), `AGENDA_TTY` (a file of answers, defaulting to a path that does not exist). The `origin:` grep is now case-insensitive so it covers the env form too. | All three were **proven to bite** by breaking them on purpose. **T07 drives `cockpitd` the same way and must add a guard with any new seam**: a browser is the one side effect a test cannot take back, and a prompt on the real `/dev/tty` would block the suite forever on a machine with a person at it. |
| 2026-08-28 | 📌 **The CLI suite builds its child's environment from NOTHING, not from `process.env`.** This repository's suites are routinely run by an agent, and an inherited `CLAUDECODE` turns every `add`, `rm`, `color` and `setup` into a refusal — a green-looking suite that tested the refusal path twenty times and the real path never. | **Any later suite that spawns a cockpit command must do the same.** The same trap is waiting for T07, whose daemon reads `CLAUDECODE`-adjacent state. Whitelist `PATH`, `HOME`, `TZ` and the seams; inherit nothing else. |
| 2026-08-28 | 📌 **`agenda add` already writes T07's cache shape.** Its immediate first fetch stores `{ fetchedAt, events, error: null }` on success and `{ ...describeError(err), since }` on failure — asserted end to end, including that a scope 403 lands as kind `auth` with the scope signal in `reason` and no response body in the file. | **T07 writes the same shape and may copy the call.** Its own task doc gives it a separate copy on purpose (the daemon must never die of it), so this is a reference, not a module to import. `since` is set on the first failure so the daemon has one to preserve. |
| 2026-08-28 | 📌 **`cockpit-test` is green again — 134 assertions, ALL PASS**, re-measured this session at `b1ee3b3`. `f180213` ("run the cockpit suite ~1.65x faster, and fix fixtures broken by 2ce144d") repaired the eleven failures the row below records; nothing was reverted. | **Supersedes the row below — T07 is no longer blocked on a red baseline.** The number to hold is **134**: not 126, not 117. Re-measure anyway before touching `cockpitd.mjs`; this figure has now moved four times mid-plan. |
| 2026-08-28 | 🐞 **Two defects in T04, both about what the user is TOLD.** The redirect's `state` was checked only on the branch carrying a code, so a stray local `?error=access_denied` ended a sign-in blaming Google for a refusal that never happened; and `describeError` dropped a scope signal `classifyError` had read from the raw body, drawing `sign-in expired` for a consent-checkbox 403. | Both fixed in T04 review, 404 → **412**. **The cache contract is unchanged for T05/T07** — still `describeError(err)`, and the response body still never rides into `agenda-cache.json`; the scope signal is named in `reason` instead. |
| 2026-08-28 | 🐞 **`cockpit-test` is RED on `main`, and not because of this plan.** 116 ok / 11 FAIL at `2ce144d` ("Keep default panes for an agent left at a non-repo dir"); its parent `2390851` is green at **126**. Failures cluster in the cwd-migration sections (9, 9b, 9c) and the quit-revdiff healer (10). | Left alone — out of scope, and whose history it is is the user's call. **T07 must not start on a red suite**: fix or revert `2ce144d` first, and re-measure. The baseline to beat is 126, not the 117 recorded earlier. |
| 2026-08-28 | 📌 **The client hands the cache `describeError(err)`, not a bare string.** `classifyError` returns the four words the task doc names, but the model tells "sign-in expired" from "calendar permission not granted" by grepping `reason`/`code`/`detail` — so the object is the contract, and it is asserted end to end through `renderAgenda`. | **T05/T07 store `describeError(err)` as the cache entry's `error`.** A bare `classifyError` string still renders, but a scope 403 would then read `sign-in expired` — right kind, wrong sentence, and the wrong fix on screen. |
| 2026-08-28 | 📌 **"Never reaches Google" is now measured, not asserted.** All three suites pass under `sandbox-exec` with outbound denied (a real `fetch` to googleapis.com in that sandbox gets `ENOTFOUND`), 560 assertions. `run.sh` also greps every test's `origin:` and fails on any that is not the loopback stub. | A network call that crept in would otherwise pass silently on a connected machine. Re-run it after touching the client: `sandbox-exec -f <profile> bash -c '<the three suites>'` — the four-line profile is in this commit's message. |
| 2026-08-28 | 📌 **`signIn` has no default browser opener and refuses without one**, and `accessToken` takes `now`/`timeoutMs` as parameters. A missing spy in a test would otherwise open a real consent screen, and a 10s hard-coded timeout is untestable in a millisecond suite. | **T05 passes `/usr/bin/open`; T07 passes its own tick as `now`** rather than letting the default clock read stand (DESIGN 3.4). `AGENDA_DRY_RUN` stays the CLI's to read — the module takes a `dryRun` flag. |
| 2026-08-28 | 🐞 **Three ways a line escaped `renderAgenda`'s own contract**, found in T03 review: the **header was never clipped** (`pad` only grows, so under ~11 columns it overran `width`); **two rows could both wear `NOW`** (matched by event id, and Google reuses one id across calendars); an **event title could inject control sequences** (a newline painted a 7th terminal line out of 6 array entries). | All fixed, 288 → **300**. **Untrusted text is now sanitised where it enters the drawing** — T04/T05 need not re-strip titles, but must not undo it. The width sweep now starts at 1, not 12. |
| 2026-08-28 | 📌 **`cockpit-welcome.mjs` still has its OWN `clip`, and it behaves differently** — it does a raw `s.slice()`, which counts escape bytes as columns and can cut an escape in half. The model's exported one walks the string. T03 exported the helpers but left the pane's copies, correctly, as T06's work. | **T06's swap is a behaviour change, not a move**: notes carrying escapes will clip differently and better. Expect `notes-test` (39) to stay green, but read it rather than assuming. |
| 2026-08-28 | 🔄 **The scope line says `TODAY · Wed 26 Aug`, both scopes labelled** — the user's call, over the bare date the T03 and T06 task docs both draw. DESIGN §2.3 already said this and now stands unamended. | **T06's mock-up in its own task doc is out of date**: follow DESIGN §2.3 and the rendered output, not that block. The word is what changes meaning, so it is drawn bright and the date beside it dim. |
| 2026-08-28 | 📌 **T03 fixed the cache's error contract, and T04 has to write it.** `error` is a bare string or `{ kind }` — `network` \| `auth` \| `gone` \| `unknown`. Scope-insufficiency travels as kind `auth` with `reason`/`code`/`detail` naming SCOPE (or kind `scope`). | `auth`/`gone` are loud and drop that calendar's rows; **`network` AND `unknown` are both quiet** — a malformed 200 is nothing the user can act on, and §2.7 is loud only when a command fixes it. An unrecognised kind reads as transient. |
| 2026-08-28 | 📌 **`renderAgenda` and `agendaHeight` take `tz`, which the task doc's interface omitted.** Without it the column draws in UTC on a Warsaw machine; reading `Intl`'s default zone inside the model is exactly the environment read §3.1 forbids and the purity grep cannot see. | **T05 and T06 must pass the machine's zone** from the impure side, beside `now`. It defaults to UTC, so forgetting it is a wrong column rather than a crash — nothing will fail a test for you. |
| 2026-08-27 | 🐞 **`normaliseEvent` leaked two ways.** A `responseStatus` naming an Object property (`constructor`, `toString`) walked the prototype and returned a **function** as `reply`; a `dateTime` with **no offset** was parsed in the **machine's** zone — the §3.1 read the purity grep cannot see, because it happens inside `Date.parse`. | Both fixed in T02 review, +6 assertions (157 → **163**). An offset-less stamp is now placed by its own `start.timeZone`, else the calendar's `tz`. **T03 may trust `reply` is one of the five strings.** |
| 2026-08-27 | 📌 **`chooseEvents(events, now)` throws `RangeError` when `now` is not a real epoch** — `undefined`, `NaN` or a string all reach `Intl` as an invalid date. (`null` silently means 1970.) It is a caller contract, not data, so the model deliberately does not guess. | **T03/T06 must hand it `Date.now()` from the impure side**, never a number read out of a state file or a cache. The failure mode is a thrown renderer and a blank pane, which §2.7 says must not happen. |
| 2026-08-27 | 📌 **`chooseEvents` takes calendar-add order from the ORDER OF ITS INPUT ARRAY.** All-day rows have no start time to sort by, and the signature carries no calendar list, so nothing else can rank them. The timed sort is by start alone and relies on `Array.sort` being stable. | **T04 and T06 must concatenate each calendar's events in `state.calendars` order** before calling it. Get that wrong and the all-day rows shuffle, and two 17:30s swap places between refreshes. |
| 2026-08-27 | 📌 **A day is not always 24 hours, and an all-day event is a civil date, not an instant.** Warsaw's 25 Oct 2026 runs 25 hours; midnight+24h lands an hour early and throws that evening onto the wrong day. The model resolves each midnight with the offset in force *at the answer*, two passes. | `dayBounds(now, { tz })` is exported for exactly this and returns `todayStart`/`tomorrowStart`/`dayAfterStart`. **T04's fetch window (§2.5) must use it**, never `now ± 24h`. |
| 2026-08-27 | 📌 **DESIGN §3.1's purity grep is now armed and was proven to bite**: adding a filesystem import and a clock read to `cockpit-agenda-model.mjs` turned `agenda-test` red, removing them turned it green. `run.sh` needed no edit — it globs `*.test.mjs` and self-arms on the file appearing. | **T03 extends the same file and inherits the check.** If it ever fails the fix is to move the code out, never to relax the grep. agenda-test is now **157** assertions. |
| 2026-08-27 | 🐞 **`withLock` was not reentrant, and one lock over three files makes nesting ordinary.** A compound write (add a calendar, prime its cache) took **5035ms** and the inner call broke the outer's own lock as stale and unlinked it — the rest of the transaction ran unlocked. | Fixed in T01 review by depth-counting; the test asserts the lock is still held after the inner calls, not just that it was fast. **T04/T05/T07 may now wrap several store writes in one `withLock`** — that is what it is for. |
| 2026-08-27 | 📌 **Two guards in `agenda-test/run.sh` read stronger than they were.** The real-`~/.claude/cockpit` check compared filenames only; the zero-dependency grep missed single quotes and `await import()` and was matching prose in a comment. | Both hardened in T01 review. The real-dir check now stats `agenda*` (name+size+mtime, never contents) — **not the whole directory, because a live cockpit rewrites `terminals.json` constantly** and would fail the suite spuriously. agenda-test is now **65** assertions, not 60. |
| 2026-08-27 | 📌 **`spikes/agenda-test/run.sh` exists and is the plan's test command; baseline 60 assertions.** It runs every `*.test.mjs` beside it in its own fresh `COCKPIT_DIR`, then checks in bash that nothing reached the repo or the real `~/.claude/cockpit`. | Later tasks add a suite file rather than editing `run.sh`. **DESIGN §3.1's purity grep is written but skipped** — it prints `the pure model does not exist yet` until T02 creates `cockpit-agenda-model.mjs`, then arms itself. T02 must see it turn green. |
| 2026-08-27 | 🔄 **`agenda setup` takes the path to Google's downloaded JSON**, not two typed values — the user's decision once T00 showed the real file is nested. | DESIGN §2.9 and §7 amended. The parse is a pure `parseGoogleClient(text)` in the model, added and tested by **T05** — **supersedes** the row below routing it to T04. T01's store stays dumb and receives normalised values. |
| 2026-08-27 | 📌 **A 403 carrying `ACCESS_TOKEN_SCOPE_INSUFFICIENT` must classify as `auth`, not `gone`.** The consent checkbox finding below has a behavioural consequence the plan had wrong. | DESIGN §2.7 amended; T04 gains the rule and two tests. Rendering `calendar gone · agenda rm` for a consent mistake would destroy a working configuration and fix nothing. |
| 2026-08-27 | 📌 **The cockpit-test baseline moved again**, 108 → 117, from two footer/click-diff-mode commits landing on `main` (`0562a6a`, `8339211`). Re-measured on `8339211`: notes-test 39, cockpit-test 117, both ALL PASS. | Third time this number has drifted mid-plan. **Re-measure it at the start of any task that touches `cockpitd.mjs` rather than trusting the recorded figure** — the rule that matters is ALL PASS with no count going down. |
| 2026-08-27 | ✅ **Both Google accounts connect.** The personal and the company account each completed the loopback+PKCE flow by hand and returned their calendar lists; both issued a refresh token. Neither was blocked by an admin. | The risk that gated the whole plan (§7, T00) is retired — no private-iCal fallback is needed. T01+ may proceed on the assumption both accounts work. |
| 2026-08-27 | 📌 **Publishing status *Testing* works for connecting and issues refresh tokens.** The ~7-day refresh-token expiry in *Testing* (DESIGN §2.9, still 📌 below) is **not** settled — it needs a week of elapsed real time to see. | Partially supersedes the "believed, not verified" row: *Testing* is confirmed usable now. If tokens do expire weekly, §2.7's loud `sign-in expired · agenda add work` line already covers it — no redesign, which is why this stays a risk, not a blocker. |
| 2026-08-27 | 📌 **Two console gotchas, both user-action not code:** (a) an account not on the OAuth screen's **Test users** list gets a hard "Access blocked — not verified" with no way past; it must be added. (b) Google's **granular consent** shows a per-scope checkbox — leaving the calendar box unticked yields a token that 403s `calendarList` with `ACCESS_TOKEN_SCOPE_INSUFFICIENT`. | T04 and `agenda setup` docs must tell the user to add every account as a test user and to tick the calendar permission. The 403 is indistinguishable from a scope bug but is a consent choice. |
| 2026-08-27 | 🐞 **Google's downloaded Desktop-client JSON is nested**, not flat: `{ "installed": { client_id, client_secret, … } }` (snake_case). The probe first assumed the task doc's flat `{ clientId, clientSecret }` and rejected the real file. | Probe now reads `installed`/`web`/flat and both key styles. **T04's store/client (`agenda-client.json`, DESIGN §3.5) must accept the nested shape too**, or paste-the-file-Google-gave-you (§2.9) fails the same way. |
| 2026-08-26 | 📌 **The plan was written against `c9e4592`; six commits landed on `origin/main` mid-session** (reflog-watch diff reload, parked-worktree following, a Cmd+C change). `cockpitd.mjs` grew ~185 lines and `cockpit-test` grew 82 → 108 assertions. | T07's hook was re-checked against the new code: `onExit()` still exists and is still called from `reconcile()` on the list transition. Baselines above are re-measured, not inherited. |
| 2026-08-26 | 📌 **Refresh-token expiry is believed, not verified.** A Google client left in *Testing* status is thought to expire refresh tokens after ~7 days; *In production* is thought to fix it, at the cost of an "unverified app" warning. From memory, not measured. | T00 must confirm which publishing status works and record it here. If weekly re-auth is unavoidable, DESIGN §2.7's loud `sign-in expired` line already covers it — no redesign. |
| 2026-08-26 | 📌 **Google's OAuth endpoints verified live** from `accounts.google.com/.well-known/openid-configuration`: auth `.../o/oauth2/v2/auth`, token `oauth2.googleapis.com/token`, revoke `.../revoke`, and `S256` is in `code_challenge_methods_supported`. | PKCE loopback is confirmed available. T04 hard-codes these rather than fetching discovery at runtime. |
| 2026-08-26 | 📌 **Node v24.2.0 has everything needed built in** — `fetch`, `node:http.createServer`, `webcrypto.subtle.digest`, `crypto.randomBytes` — all confirmed on this machine. `Intl` reports `Europe/Warsaw`. | The zero-dependency rule (DESIGN §5) holds. No task should reach for a library; if one seems to, that is a question for the user. |
| 2026-08-26 | 📌 **`/usr/bin/cal` exists and the cockpit *prepends* its bin dir to PATH** (`bin/cockpit-layout.sh:77`), so a `cal` symlink would shadow the month grid in every cockpit terminal **and in every agent**, which inherits that PATH. | 🔄 The user renamed the command to `agenda`. DESIGN §2.2. |
| 2026-08-26 | 📌 **Baseline test measurement**: `spikes/notes-test/run.sh` 39 assertions ALL PASS, `spikes/cockpit-test/run.sh` 117 assertions ALL PASS, on a clean `main` at `8339211`. | Any session that reddens either has broken something it did not mean to touch. Both are part of the test command (DESIGN §5). |
| 2026-08-26 | 📌 **`cockpitd.mjs` already detects the return to the fleet list** — `onExit()`, called from `reconcile()` on the list→agent transition, at an 800ms poll. | T07's "refresh when I come back" hook exists; it does not need a new detector. |
